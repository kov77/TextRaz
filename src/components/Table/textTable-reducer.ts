import { createSlice, Dispatch } from "@reduxjs/toolkit"
// import {textApi} from "../../App/api";


const initialState = {
}

const slice = createSlice({
    name: "textTable",
    initialState,
    reducers: {

    },
})
export const textTableReducer = slice.reducer

export const {

} = slice.actions


//Thunk

